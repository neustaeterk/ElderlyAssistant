<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.kenton.elderlyassistant.EditReminderActivity"
    tools:showIn="@layout/activity_edit_reminder"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Edit the medication name (if needed):"
        android:textSize="25dp"
        android:textColor="#000080"
        android:id="@+id/textTitle" />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/medicationName"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/timePicker"
        android:layout_below="@+id/textTitle"
        android:textSize="25dp"
        android:singleLine="true" />

    <TimePicker
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/timePicker"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/medicationName" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="RETAKE A PHOTO OF MEDICATION"
        android:textSize="25dp"
        android:textColor="#FFFFFF"
        android:id="@+id/takePhotoButton"
        android:layout_below="@+id/timePicker"
        android:layout_alignParentStart="true"
        android:background="@drawable/button_turquoise"/>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="UPDATE REMINDER"
        android:textSize="25dp"
        android:textColor="#FFFFFF"
        android:id="@+id/addMedicationButton"
        android:layout_below="@+id/takePhotoButton"
        android:layout_alignParentStart="true"
        android:background="@drawable/button_turquoise"/>

</RelativeLayout>

</ScrollView>